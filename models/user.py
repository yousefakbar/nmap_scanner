# TODO: Autheticate user and track user scans
class User():
    def __init__(self, username, name, previous_scans):
        self.username = username
        self.name = name
        self.previous_scans = previous_scans
